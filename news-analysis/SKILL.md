---
name: news-analysis
description: 每日热点新闻分析并写入 Obsidian。当用户提到"新闻汇总"、"今日热点"、"热点新闻"、"新闻分析"、"帮我搜一下今天的新闻"时使用此 skill。
---

# News Analysis Skill

将每日热点新闻搜索并整理写入 Obsidian。

## 安装方法

```bash
npx skills add https://github.com/Rinsonlaw/law-skills.git
```

## 工作流程

### 第一步：获取 Obsidian 路径

使用 `/obsidian` skill 获取本机 Obsidian vault 路径。

### 第二步：搜索热点新闻

检索工具选择顺序：
1. 如果当前模型是MiniMax，则使用 `mmx search query` 搜索。

```bash
DATE=$(date +%Y-%m-%d)
mmx search query "国际新闻 $DATE"
mmx search query "国内时政 $DATE"
mmx search query "财经新闻 $DATE"
mmx search query "科技AI $DATE"
```

2. 如果当前模型是GPT，则调用 `web_search` 工具联网搜索以下内容：

> **提示：** 每次调用 `web_search` 时，需先获取当天日期（如 `2026-05-06`），拼接为完整搜索词后再调用。

```bash
# 获取当天日期
DATE=$(date +%Y-%m-%d)

# 调用 web_search 工具搜索（替换下方占位符）
web_search "国际新闻 ${DATE}"
web_search "国内时政 ${DATE}"
web_search "财经新闻 ${DATE}"
web_search "科技AI ${DATE}"
```

3. 如果没有联网搜索能力，停止任务并提示用户：当前环境无法获取最新新闻，需要启用联网搜索或提供新闻来源。

### 第三步：整理新闻内容

对每条热点新闻：
1. 写 100 字以内的核心内容摘要
2. 保留原文链接
3. 标注新闻来源和时间

### 第四步：写入 Obsidian

1. 确定写入路径：`{Obsidian路径}/news/`
2. 文件命名格式：`$(date +%Y-%m-%d_%H:%M:%S)_{热点新闻}.md`
3. 按以下格式写入：

```markdown
# 今日热点新闻30条 | $(date +%Y-%m-%d)

## 🌏 国际动态
### [序号]. [新闻标题]
**摘要**：[100字以内核心内容]
**来源**：[媒体名称1](原文链接1) | [媒体名称2](原文链接2) | ...

---

...

---

## 🇨🇳 国内时政
### [序号]. [新闻标题]
**摘要**：[100字左右核心内容]
**来源**：[媒体名称1](原文链接1) | [媒体名称2](原文链接2) | ...
---

...

---

## 💰 财经新闻
### [序号]. [新闻标题]
**摘要**：[100字左右核心内容]
**来源**：[媒体名称1](原文链接1) | [媒体名称2](原文链接2) | ...
---

...

---

## 🤖 科技·AI
### [序号]. [新闻标题]
**摘要**：[100字左右核心内容]
**来源**：[媒体名称1](原文链接1) | [媒体名称2](原文链接2) | ...
---

...

---

## 📈 趋势归纳

[对今日新闻的趋势分析，分点叙述，每个点不超过100字]

---

*整理时间：$(date +%Y-%m-%d\ %H:%M:%S)*
*整理工具：{实际使用的检索工具} + {当前模型} + OpenClaw*

```

## 注意事项
- 所有新闻的序号要从1开始，依次递增到30
- 摘要需精炼，控制在 100 字左右
- 所有链接必须保留原文地址
- 趋势归纳部分要有深度分析，不只是罗列
- 文件名中时间精确到秒，避免重复

## 错误处理

- 若 Obsidian vault 不存在，提示用户检查路径
- 若 news 文件夹不存在，自动创建：`mkdir -p "{Obsidian路径}/news"`
- 若 mmx search 无结果，输出"今日未找到热点新闻"并退出
